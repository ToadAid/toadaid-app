# Stage B2-P4B-P13 — Foreground pond ecology

## Binding

Parent commit: `a8c43cf12243854d87f55e43d5e4b80254c17348`

Feature branch: `feat/stage-b2-p4b-p13-foreground-pond-ecology`

Canonical architecture commit:
`bc7a971dfb243f0aa4417da6cef85cc56204f783`

## Goal

Give the existing procedural Pond world a stronger near-field ecological frame
that connects the floating Lore Land composition to the approved sci-fi
desktop direction.

This is decorative presentation only. The ecology does not represent a live
world, Lore Land holding, scope relationship, agent state, resource inventory,
or canonical current fact.

## Explicit non-goals

- no fixture, contract, agent descriptor, scope, or relationship change;
- no world selection, discovery, membership, admission, navigation, or input;
- no repository asset admission or use of operator reference pixels;
- no Bridge, MCP, provider, network, persistence, messaging, delivery,
  approval, execution, wallet, signing, trading, publication, or deployment.

## Exact scope

Files:

- `BUILD_LIST.md`
- `docs/stage-b2-p4b-p13-foreground-pond-ecology.md`
- `ui/pond-world.js`

Runtime surface: existing deterministic Three.js presentation inside the
already-bounded local Pond stage.

Network scope: package installation and GitHub repository workflow operations
only. The Pond runtime receives no network capability.

## Procedural composition

The cut adds:

- fourteen deterministic notched lily pads in two shared material tones;
- three two-layer lotus blooms with a bounded luminous center;
- twenty-six deterministic reeds and tips rendered through two instanced
  meshes;
- slight near-field ecology response to the existing neutral-reset pointer;
- restrained lotus bob and rotation driven by the existing elapsed-time path.

The ecology uses code-native geometry and shared materials. No bitmap, model,
texture, reference pixel, loader, dependency, or asset admission is added.

## Lifecycle and performance boundary

The P4B-P8 policy remains unchanged:

- desktop, narrower, and reduced-motion frame/pixel profiles are unchanged;
- actual-stage resize observation is unchanged;
- hidden/offscreen pause and resume are unchanged;
- pointer leave still returns to the neutral target;
- disposal and WebGL-loss fallback are unchanged.

Reduced-motion preference continues to scale all elapsed-time movement to the
established P4B-P8 value. Reeds are instanced to bound their draw-call cost.
This cut makes no measured FPS, GPU, memory, mobile, or cross-platform claim.

## Trusted sources and precedence

Canonical ToadAid architecture at the bound commit governs ecosystem law. The
exact B1 fixture and local presentation contracts govern rendered facts.
Operator images under `/home/tommy/Pictures/toadaid-app/` remain visual
direction only; they cannot establish runtime truth, scope, ownership, or
authority and are not copied into the repository.

## Authority ceiling

Authority: `none`.

The renderer can emit decorative pixels only. It cannot establish identity,
scope, membership, admission, audience, release, capability, grant, approval,
execution, or canonical current state. All existing static gates remain OFF.

## Validation and completion evidence

- `npm ci --ignore-scripts --no-audit --no-fund`;
- `npm run typecheck`;
- JavaScript syntax check for `ui/pond-world.js`;
- P4B-P8 deterministic render-policy self-test;
- locked Tauri Rust check;
- exact changed-path and whitespace review;
- native Linux launch and visual observation of both initial and corrected
  ecology placement, with no observed clipping, fallback, or launch fault.

## Activation

Procedural ecology presentation: **INCLUDED**.

Live capability, agent, host integration, or consequence activation:
**NOT_INCLUDED**.

Repository asset admission or loading: **NOT_INCLUDED**.
