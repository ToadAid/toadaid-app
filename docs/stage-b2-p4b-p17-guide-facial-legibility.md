# Stage B2-P4B-P17 — Guide facial legibility

## Binding

Parent commit: `14a08fc7d5e7dffbe8705603ed8c81616169c45a`

Feature branch: `feat/stage-b2-p4b-p17-guide-facial-legibility`

Canonical architecture commit:
`bc7a971dfb243f0aa4417da6cef85cc56204f783`

## Goal

Improve the existing procedural central guide's facial legibility at desktop
presentation scale without changing its calm guardian character, central
composition role, or established lifecycle behavior.

The guide remains an inert visual fixture. A more legible face is not an agent
invocation, identity proof, presence claim, admission fact, capability, or
authority signal.

## Explicit non-goals

- no change to fixture facts, safety copy, contracts, descriptors, or agent
  semantics;
- no character customization, expression state, lip sync, speech state, or
  conversational behavior;
- no asset admission, copied reference pixels, texture, model, loader, or new
  dependency;
- no measured performance, device-class, cross-platform, or accessibility
  conformance claim;
- no Bridge, MCP, provider, network, persistence, routing, delivery, approval,
  execution, wallet, signing, trading, publication, or deployment.

## Exact scope

Files:

- `BUILD_LIST.md`
- `docs/stage-b2-p4b-p17-guide-facial-legibility.md`
- `ui/pond-world.js`

Runtime surface: existing local procedural Three.js presentation only.

Network scope: package installation and GitHub repository workflow operations
only. The Pond runtime receives no network capability.

## Procedural refinement

The cut adds eleven small procedural presentation forms to the existing guide:

- two soft brow ridges;
- two thin eye rims;
- two subtle cheek planes;
- two nostrils;
- two mouth corners;
- one shallow throat/bib plane.

The existing render loop provides a restrained throat-breath and brow drift
using its current elapsed-time source. These details inherit the P4B-P8 render
lifecycle, pause when the renderer pauses, and slow with the established
reduced-motion timing. They do not create a new timer, observer, event source,
or state machine.

All forms use existing procedural materials and primitives. The operator's
original desktop target, imagined scenes, and Lore Land deeds remain visual
references outside the repository; no reference pixels or assets are copied,
loaded, admitted, or assigned authority.

## Lifecycle boundary

The P4B-P8 lifecycle remains intact:

- responsive render-profile and density selection are unchanged;
- document-hidden and offscreen states still pause rendering;
- reduced-motion, pointer reset, disposal, and WebGL-loss fallback are
  unchanged;
- the guide remains part of every full, balanced, and essential world tier.

## Trusted sources and precedence

Canonical ToadAid architecture at the bound commit governs ecosystem law. The
exact B1 fixture and local presentation contracts govern rendered facts. The
existing P4B-P8 lifecycle governs presentation work. Visual character cues
cannot establish or reinterpret an authority-relevant fact.

## Authority ceiling

Authority: `none`.

This cut can shape and animate inert local geometry only. It cannot establish
identity, scope, membership, admission, audience, release, reachability,
routing, delivery, capability, grant, approval, execution, or canonical
current state. All static gates remain OFF.

## Validation and completion evidence

- `npm ci --ignore-scripts --no-audit --no-fund`;
- `npm run typecheck`;
- JavaScript syntax check for the renderer;
- deterministic render-policy self-test;
- locked Tauri Rust check;
- exact changed-path and whitespace review;
- native Linux full-profile launch and visual observation confirming that the
  guide face remains legible, calm, centered, and unobstructed at the observed
  desktop scale.

The native observation is qualitative Linux evidence only. It is not measured
performance, cross-platform, or device-family proof.

## Activation

Procedural presentation refinement: **INCLUDED**.

Live capability, agent, host integration, or consequence activation:
**NOT_INCLUDED**.

Repository asset admission or loading: **NOT_INCLUDED**.
