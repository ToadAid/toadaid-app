# Stage B2-P4B-P11 — Guide presence choreography

## Binding

Parent commit: `3f637fc9cc8798735125e591372d93e9eeffb98b`

Feature branch: `feat/stage-b2-p4b-p11-guide-presence-choreography`

Canonical architecture commit:
`bc7a971dfb243f0aa4417da6cef85cc56204f783`

## Goal

Give the existing procedural guide a stronger central, ceremonial presence in
the Pond composition without adding an asset, dependency, source of truth,
capability, agent behavior, or authority surface.

This is a presentation-only refinement over the P4B-P10 cinematic world. The
guide remains a visual specialist-agent projection and is not a live agent,
invocation target, authority holder, or representation of canonical current
state.

## Exact source scope

- `BUILD_LIST.md`
- `docs/stage-b2-p4b-p11-guide-presence-choreography.md`
- `ui/pond-world.js`

No dependency, host, contract, fixture, asset, or canonical-governance file
changes are included.

## Visual composition

The cut adds only deterministic, code-native presentation:

- a layered procedural lily dais beneath the guide;
- ten translucent leaf petals and three low concentric energy rings;
- a faint additive light column contained within the guide composition;
- a robe-colored hood rim that strengthens the guardian silhouette;
- three small modeled fingertips on each existing hand;
- a brief deterministic blink and restrained dais pulse/rotation choreography.

All motion remains driven by the existing elapsed-time path. Reduced-motion
preference continues to scale elapsed motion to the established P4B-P8 value.

## Performance and lifecycle boundary

The P4B-P8 render policy remains unchanged:

- desktop pixel ratio and frame-rate caps remain bounded;
- narrower and reduced-motion profiles remain unchanged;
- actual-stage resize observation remains unchanged;
- hidden/offscreen pause and resume remain unchanged;
- disposal and WebGL-loss fallback remain unchanged.

The cut adds a bounded number of simple procedural meshes and no runtime asset
loading. It makes no measured FPS, GPU, memory, mobile, or cross-platform
claim. Native observation proves only that the candidate launched and rendered
on the observed Linux host.

## Authority ceiling

Authority: `none`.

The cut cannot establish identity, scope, membership, admission, Release,
route, delivery, grant, approval, execution, persistence, or canonical world
state. It cannot invoke an agent, call a tool, connect to Bridge or MCP, access
a wallet, sign, trade, publish, deploy, or mutate a repository at runtime.

## Trusted sources and precedence

Canonical ToadAid architecture at the bound commit governs ecosystem law.
Local repository contracts and fixtures govern the semantics already presented
by Pond. The procedural renderer supplies only decorative pixels and cannot
override either source.

## Activation

Procedural presentation refinement: **INCLUDED**.

Live capability or agent activation: **NOT_INCLUDED**.

Repository asset admission or loading: **NOT_INCLUDED**.

## Verification

- `npm ci --ignore-scripts --no-audit --no-fund`;
- `npm run typecheck`;
- JavaScript syntax check for `ui/pond-world.js`;
- P4B-P8 deterministic render-policy self-test;
- locked Tauri Rust check;
- native Linux launch and visual observation;
- exact changed-path and whitespace review.
